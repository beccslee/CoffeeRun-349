{
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-plugin-react": "^7.26.0"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "build": "npm run clean && npm install && npm build"
  }
}
